{
  "name": "handpress",
  "license": "AGPL-3.0-only",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "npm run ocr-assets && vite",
    "build": "npm run ocr-assets && tsc && vite build",
    "ocr-assets": "node tools/ocr-assets.mjs",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name handpress"
  },
  "devDependencies": {
    "tsx": "^4.23.5",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "wrangler": "^4.120.0"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "fontkit": "^2.0.4",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.2.108",
    "tesseract.js": "^7.0.0"
  }
}
